# Copy to .env and fill in real values. Never commit the real .env.

# --- Server ---
PORT=4000
NODE_ENV=development
CLIENT_ORIGINS=http://localhost:5173,http://localhost:5174
# Short-term conversation memory: number of recent messages fed back each turn
CHAT_HISTORY_LIMIT=20

# --- OpenAI ---
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# --- Supabase ---
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SECRET_KEY=sb_secret_xxx        # server-side only, bypasses RLS
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
DATABASE_URL=postgresql://postgres.YOUR-PROJECT:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres

# --- GoHighLevel (GHL) ---
GHL_API_KEY=your-ghl-api-key
GHL_LOCATION_ID=your-location-id
# Real booking + deposit form URL (leave empty until you have it; the bot never
# invents a link when blank)
BOOKING_FORM_URL=
