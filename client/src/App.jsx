import ChatApp from './components/ChatApp'

// The client app IS the chat experience (standalone page, embeddable on the
// website / linked from Instagram & WhatsApp).
export default function App() {
  return <ChatApp />
}
